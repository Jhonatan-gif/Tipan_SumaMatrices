#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    //Pedir dimensiones
    int fil , col, i, j;
    printf("Ingrese el numero de filas de la matriz: ");
    scanf("%d", &fil);
    printf("Ingrese el numero de columnas de la matriz: ");
    scanf("%d", &col);

return 0;
}